import { SealedPreview as SealedScreen } from '@/components/prototype/screens/SealedPreview';

/**
 * Preview page for the merged Sealed screen concept.
 * PinGate-protected via App.tsx routing.
 */
export default function SealedPreviewPage() {
  return <SealedScreen />;
}
